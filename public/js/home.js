const options = {
    strings: ["Ask your doubts", "Get answers!", "Have a great day!"],
    typeSpeed: 50,
    backSpeed: 25,
    backDelay: 1250,
    loop: true,
    loopCount: Infinity,
    smartBackspace: true,
};

new Typed(".typings", options);
